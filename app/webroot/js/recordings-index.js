$( document ).ready(function() {
	/*
    var popup = $("#view_dialog").dialog({
        autoOpen: false,
        title: 'Recording Details',
        height: 400,
        width: 800,
        resizable: true,
        modal: true,
        buttons:
        {
            "Ok": function() {
                $(this).dialog("close");
            }
        }
    });	


	$("a[id^='rec']").click( function(event) {
		event.preventDefault();
		
		var url = location.protocol + '//' + location.host + '/Recordings' + $(this).attr('href');
		popup.load(url, function (){
            popup.dialog('open');
        });
        //return false;

    });
*/
	
	
});